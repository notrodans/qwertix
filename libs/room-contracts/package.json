{
	"name": "@qwertix/room-contracts",
	"version": "0.1.0",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "tsc --noEmit -p tsconfig.json && eslint . && biome check .",
		"lint:fix": "tsc --noEmit -p tsconfig.json && eslint . --fix && biome check --write ."
	},
	"devDependencies": {
		"@qwertix/tsconfig": "workspace:*",
		"typescript": "catalog:",
		"vite-tsconfig-paths": "catalog:",
		"vitest": "catalog:"
	}
}
