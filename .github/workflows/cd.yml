name: CD

on:
  workflow_run:
    workflows: ["Docker Publish"]
    branches: [main]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Deploy to production
        run: |
          echo "Deploying to production with tag ${{ github.sha }}..."
          # Пример команды для деплоя через SSH (раскомментируйте и настройте при необходимости):
          # ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
          #   "IMAGE_TAG=${{ github.sha }} docker stack deploy -c docker-compose.stack.yml qwertix --with-registry-auth"

