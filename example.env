#### PUBLIC (Plaintext in .env or Docker Compose)
NODE_ENV=development
PORT=3009
DB_HOST=localhost
SITE_BASE_URL=http://localhost:3006
VITE_API_URL=/api
VITE_WS_URL=/api
VITE_RESULT_HASH_SALT

#### SECRETS (Managed via Doppler Admin Panel)
# The following variables should be set in Doppler, NOT in .env:
# DATABASE_URL
# JWT_SECRET
# RESULT_HASH_SALT

#### DOPPLER CONFIG
# For local dev: run `doppler setup`
# For Docker/Production: set this secret
# DOPPLER_TOKEN=dp.pt.xxx
